apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kerberos-io.fullname" . }}-test-connection"
  labels:
    {{- include "kerberos-io.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kerberos-io.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
